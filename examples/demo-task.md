# Demo Task

Add password complexity validation to the signup flow.

Requirements:

- Keep the existing email validation behavior.
- Require passwords to include at least one uppercase letter, one lowercase letter, and one number.
- Add tests for missing uppercase, missing lowercase, missing number, and a valid complex password.
- Keep the implementation small and easy to review.

Suggested demo agents:

- `mock-codex`
- `mock-claude`

Suggested test command:

```bash
npm test
```
